package edu.trinity.scalavis

/**
 * A node in the graph that stores values.
 * 
 * @author Mark Lewis
 */
class ValueNode {
    private var inputBuilder:Builder=null
    private var inputIndex:Int=0
    private var outputBuilders:Seq[Builder]=Nil
    private var outputIndex:Int=0
}
package edu.trinity.scalavis

import ScalaVis._

// An axis is really (String,IndexedSeq[Double],AxisTransform)

class Plot(contents:Seq[Plottable]) extends Element {
    def apply(x:IOList,y:IOList):IOList = {
        // TODO : draw the plot with this data.
        IndexedSeq()
    }

}

object Plot {
    
}
